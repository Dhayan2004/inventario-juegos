/*
 * Brand DNA — CSS Variables
 *
 * Auto-generated by add-ui-kit from brand/brand.json.
 * DO NOT EDIT BY HAND. Re-run add-ui-kit if brand.json changes.
 *
 * Schema source: [memory:references#R-005]
 * Generated:     {{ generated_at_iso }}
 * Mode:          {{ mode }}
 * Preset:        {{ baseline_preset }}
 *
 * Mapping rule: brand.json.tokens.<group>.<token> → --<group>-<token>.
 *               Numeric tokens render with px suffix where appropriate.
 *
 * Citations:     [memory:references#R-005] · [memory:CONSTRAINTS.md#R10]
 *                [docs:tailwindcss] for var() arbitrary-value usage
 */

:root {
  /* ─── Colors ─────────────────────────────────────────────────────── */
  --color-primary:                       {{ tokens.colors.primary }};
  --color-primary-deep:                  {{ tokens.colors.primary_deep }};
  --color-primary-accessible-on-dark:    {{ tokens.colors.primary_accessible_on_dark | default: tokens.colors.primary }};
  --color-primary-accessible-on-light:   {{ tokens.colors.primary_accessible_on_light | default: tokens.colors.primary_deep }};
  --color-accent:                        {{ tokens.colors.accent }};
  --color-secondary:                     {{ tokens.colors.secondary | default: tokens.colors.surface_elevated }};
  --color-background:                    {{ tokens.colors.background | default: tokens.colors.surface }};
  --color-surface:                       {{ tokens.colors.surface }};
  --color-surface-elevated:              {{ tokens.colors.surface_elevated }};
  --color-surface-higher:                {{ tokens.colors.surface_higher | default: tokens.colors.surface_elevated }};
  --color-border:                        {{ tokens.colors.border }};
  --color-text:                          {{ tokens.colors.text }};
  --color-text-muted:                    {{ tokens.colors.text_muted }};
  --color-text-subtle:                   {{ tokens.colors.text_subtle }};
  --color-success:                       {{ tokens.colors.success }};
  --color-danger:                        {{ tokens.colors.danger }};
  --color-warning:                       {{ tokens.colors.warning }};
  --color-info:                          {{ tokens.colors.info | default: tokens.colors.accent }};

  /* ─── Typography — families ──────────────────────────────────────── */
  --font-display: '{{ tokens.typography.display.family }}', system-ui, sans-serif;
  --font-body:    '{{ tokens.typography.body.family }}', system-ui, sans-serif;
  --font-mono:    '{{ tokens.typography.mono.family | default: "ui-monospace" }}', ui-monospace, "Courier New", monospace;

  /* ─── Typography — weights ───────────────────────────────────────── */
  --font-weight-display-min: {{ tokens.typography.display.weights[0] }};
  --font-weight-display-max: {{ tokens.typography.display.weights[-1] }};
  --font-weight-body-regular: 400;
  --font-weight-body-medium: 500;
  --font-weight-body-semibold: 600;

  /* ─── Typography — line heights ──────────────────────────────────── */
  --line-height-body: {{ tokens.typography.body.line_height }};
  --line-height-display: 1.1;
  --line-height-tight: 1.25;

  /* ─── Shape ──────────────────────────────────────────────────────── */
  --radius-sm: {{ tokens.shape.radius_sm }}px;
  --radius-md: {{ tokens.shape.radius_md }}px;
  --radius-lg: {{ tokens.shape.radius_lg }}px;
  --border-width: {{ tokens.shape.border_width }}px;

  /* ─── Spacing ────────────────────────────────────────────────────── */
  --space-unit: {{ tokens.spacing.unit }}px;
  --space-1: calc(var(--space-unit) * 0.5);   /* 4 if unit=8 */
  --space-2: var(--space-unit);                /* 8 */
  --space-3: calc(var(--space-unit) * 1.5);   /* 12 */
  --space-4: calc(var(--space-unit) * 2);     /* 16 */
  --space-6: calc(var(--space-unit) * 3);     /* 24 */
  --space-8: calc(var(--space-unit) * 4);     /* 32 */
  --space-12: calc(var(--space-unit) * 6);    /* 48 */
  --space-16: calc(var(--space-unit) * 8);    /* 64 */
  --space-24: calc(var(--space-unit) * 12);   /* 96 */

  /* Section vertical rhythm — keyed object desde brand.json (R-005 v1.1.0) */
  --section-y-sm: {{ tokens.spacing.section_y.sm }}px;
  --section-y-md: {{ tokens.spacing.section_y.md }}px;
  --section-y-lg: {{ tokens.spacing.section_y.lg }}px;

  /* Component gap — keyed object desde brand.json (R-005 v1.1.0) */
  --gap-xs: {{ tokens.spacing.component_gap.xs }}px;
  --gap-sm: {{ tokens.spacing.component_gap.sm }}px;
  --gap-md: {{ tokens.spacing.component_gap.md }}px;
  --gap-lg: {{ tokens.spacing.component_gap.lg }}px;

  /* ─── Motion ─────────────────────────────────────────────────────── */
  --motion-duration-instant: {{ motion.durations.instant }}ms;
  --motion-duration-fast:    {{ motion.durations.fast }}ms;
  --motion-duration-base:    {{ motion.durations.base }}ms;
  --motion-duration-slow:    {{ motion.durations.slow }}ms;

  /* Easing derivado de motion.personality */
  /* energy={{ motion.energy }} → {{ motion.easing | default: "cubic-bezier(0.4, 0, 0.2, 1)" }} */
  --motion-easing: {{ motion.easing | default: "cubic-bezier(0.4, 0, 0.2, 1)" }};
}

/* ─── prefers-reduced-motion override ─────────────────────────────── */
@media (prefers-reduced-motion: reduce) {
  :root {
    --motion-duration-instant: 0ms;
    --motion-duration-fast:    0ms;
    --motion-duration-base:    0ms;
    --motion-duration-slow:    0ms;
  }
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
}

/* ─── Body baseline ───────────────────────────────────────────────── */
body {
  font-family: var(--font-body);
  font-size: 16px;
  line-height: var(--line-height-body);
  color: var(--color-text);
  background: var(--color-surface);
}

/* ─── Display headings ────────────────────────────────────────────── */
h1, h2, h3 {
  font-family: var(--font-display);
  line-height: var(--line-height-display);
  color: var(--color-text);
}

/* ─── Mono ────────────────────────────────────────────────────────── */
code, kbd, samp, pre {
  font-family: var(--font-mono);
}

/* ─── Brand utility classes ───────────────────────────────────────── */
.brand-surface         { background: var(--color-surface); }
.brand-surface-elevated { background: var(--color-surface-elevated); }
.brand-border          { border: var(--border-width) solid var(--color-border); }
.brand-radius-sm       { border-radius: var(--radius-sm); }
.brand-radius-md       { border-radius: var(--radius-md); }
.brand-radius-lg       { border-radius: var(--radius-lg); }
