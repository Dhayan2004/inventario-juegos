# Dependencies
/node_modules
/.pnp
.pnp.js
# package-lock.json NO se ignora (U-10): los jobs de CI corren `npm ci`,
# que exige el lockfile commiteado. Ignorarlo mata el CI del proyecto entero.
yarn.lock
pnpm-lock.yaml

# Testing
/coverage

# Next.js
/.next/
/out/
*.rsc

# Production
/build
/dist

# Misc
.DS_Store
*.pem
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
# keys de agente (imagen/video) — solo local, nunca ship (AP9 · R15)
.env.agents
!.env.agents.example

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Local env files
.env*.local
!.env.example

# Vercel
.vercel

# IDE
.vscode
.idea
*.swp
*.swo
*~
.sublime-project
.sublime-workspace

# OS
Thumbs.db
.AppleDouble
.LSOverride

# Claude Code
.claude/skills/**/node_modules
.claude/plugins
*.mcp.json
!example.mcp.json

# Supabase
supabase/.branches
supabase/.temp

# Plano de control (A1) — efímeros, NO versionar (plan.json + activity.log.jsonl SÍ se versionan)
.plan/.lock
.plan/.inconsistent

# autoresearch (C6) — scratch del experimento; untracked a propósito para sobrevivir al `git reset --hard`
**/autoresearch-results.tsv
**/SKILL.md.backup

# Migraciones de ontología (S3) — backups efímeros pre-migración (el ledger .forja/ontology.migrations SÍ se versiona)
.forja/ontology-backups/
